interface:
  display_name: "Private GitHub Pro Review"
  short_description: "Publish private repo reviews through GPT Pro"
  default_prompt: "Use $private-github-pro-review to publish this repository to its persistent private review mirror and run a GPT Pro review."
policy:
  allow_implicit_invocation: false
